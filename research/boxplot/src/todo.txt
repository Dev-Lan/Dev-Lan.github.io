todo:

style dropdowns

add user errors
* file load bad
* log scale can't take 0 values, or values that go across zero

add details in tutorial about log scale
move title so it doesn't overlap

add hover to selected functions in table

add more distinction to the selected section

clean up / document
* keep css class name convention consistent

* use d3 selection enter more efficiently

* * how to compute (statistically - 
		bootstrapping methods (how good is sampling strategy)
		how good is my estimator

* remove all pins
* sort table

* update data format support
* * id,t,x,y,z,a,b,c,d,e,f
* * 7,depth,8
* * 7,blah,19
* * 7,group,1